# Start a session

screen -S sessionname

# Resume a session

screen -d -R sessionname 

# to exit but keep the session running type:
Ctrl a d

# To close a screen session where all tasks are finished you can type

exit

# To see USAGE info 

top -u bioinfo

# quit top 

q

Current sessions:

screen -S Dros_mela_0000
############################# meeting notes #########################
min 
human 156G 
each node needs access to 16 threads

need one or more 256G nodes with 16Gmin per thread

min 64 ?


small job = medium memory node

large job = large mem lots of threads

which nodes are small and which are small

The
input dataset including the sample dataset in fastq format
